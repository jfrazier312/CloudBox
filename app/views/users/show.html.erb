<p id="notice"><%= notice %></p>

<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<% if is_admin? || current_user?(current_user)%>
    <p>
      <strong>Privilege:</strong>
      <%= @user.privilege %>
    </p>
<% end %>


<%= render @posts %>


<% if is_admin? || current_user?(@user) %>
    <%= link_to 'Edit', edit_user_path(@user) %>
<% end %>
