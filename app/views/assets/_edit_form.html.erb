<%= simple_form_for [user, asset], :html => { multipart: true } do |f| %>

    <%= f.input :custom_name, class: 'form-inputs' %>
    <%= f.input :description, class: 'form-inputs' %>
    <br>
    <%= f.submit "Submit", class: "btn btn-success form-actions btn-lg" %>
<% end %>
